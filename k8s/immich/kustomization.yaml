apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - immich.yaml
# Copy secret.example.yaml -> secret.yaml, fill in a password, then add it:
#   - secret.yaml   (gitignored; never commit real passwords)
