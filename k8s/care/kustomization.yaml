apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - care.yaml
  - backup.yaml
# The `care-secret` Secret is NOT listed here — it's applied straight from
# sops by `just care-deploy` (sops -d secrets/care.enc.yaml | kubectl apply
# -f -). See secret.example.yaml for its shape.
