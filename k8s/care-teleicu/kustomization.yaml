apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - care-teleicu.yaml
  - backup.yaml
# The `teleicu-secret` Secret is NOT listed here — it's applied straight from
# sops by `just care-teleicu-deploy` (sops -d secrets/care-teleicu.enc.yaml |
# kubectl apply -f -). See secret.example.yaml for its shape.
