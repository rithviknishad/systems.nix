apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - kite.yaml
# The kite-secret (JWT/encrypt keys + GitHub OAuth app + break-glass password)
# is NOT part of this build. It is stored sops-encrypted at secrets/kite.enc.yaml
# and applied at deploy time by `just kite-deploy`. See secret.example.yaml.
