apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - formance.yaml
# The `formance-secret` Secret is NOT listed here — it's applied straight from
# sops by `just formance-deploy` (sops -d secrets/formance.enc.yaml | kubectl
# apply -f -). See secret.example.yaml for its shape.
