apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ots.yaml
# The `ots-secret` Secret is NOT listed here — it's applied straight from
# sops by `just ots-deploy` (sops -d secrets/ots.enc.yaml | kubectl apply
# -f -). See secret.example.yaml for its shape.
