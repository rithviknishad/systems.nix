apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - esphome.yaml
# The esphome-secrets Secret is NOT listed here — it's created at deploy time
# by `just esphome-deploy` from the sops-encrypted secrets/esphome.enc.yaml.
